<%if params[:action] == "show" && (resource.billed? || resource.shipped?)%>
  <li class="first">
    <%= link_to image_tag("auction-hammer--arrow.png") , process_handle_load_list_path(resource),:title => "#{resource.billed? ? "发货处理" : "到货确认" }",:class => "tipsy"  %>
  </li>
<%end%>
