<%= form_for(@hand_bill) do |f| %>
  <% if @hand_bill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hand_bill.errors.count, "error") %> prohibited this hand_bill from being saved:</h2>

      <ul>
      <% @hand_bill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
