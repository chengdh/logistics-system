<%= form_for(@return_bill) do |f| %>
  <% if @return_bill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@return_bill.errors.count, "error") %> prohibited this return_bill from being saved:</h2>

      <ul>
      <% @return_bill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
